<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:cache="http://www.springframework.org/schema/cache"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd
       http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd">

    <!-- Cache -->
    <cache:annotation-driven />

    <!--
      Caffeine, since Phase 3 of the Java 21 migration. Spring 6 removed the whole
      org.springframework.cache.ehcache package, so the three beans that used to stand here
      (EhCacheCacheManager, EhCacheManagerFactoryBean over conf/catgenome/ehcache.xml, and an unused
      EhCacheFactoryBean named "cache") have no Spring 6 equivalent; ehcache.xml is deleted with them.

      One cache region is left to declare. ehcache.xml configured two, but "indexCache" was never
      reached through this manager - EhCacheBasedIndexCache used it programmatically, and Phase 7
      deleted that class outright (decision D10: feature-file headers and Tribble/Tabix indexes are
      no longer cached in memory at all). "proteinTrack" is the one @Cacheable region
      (ProteinSequenceManager.loadProteinSequence): 1,000 entries, 5 s time-to-idle. Both carry over
      exactly; the FIFO eviction policy does not, because Caffeine is Window-TinyLFU and has no FIFO
      mode - for a region bounded by a 5-second idle timeout the replacement policy never binds.

      cacheNames is deliberately fixed rather than left open. CaffeineCacheManager creates regions on
      demand when it is empty, which would turn a mistyped @Cacheable name into a silently working
      second cache; EhCacheCacheManager rejected unknown names, and this keeps that.
    -->
    <bean id="cacheManager" class="org.springframework.cache.caffeine.CaffeineCacheManager"
          p:cacheSpecification="maximumSize=1000,expireAfterAccess=5s">
        <property name="cacheNames">
            <list>
                <value>proteinTrack</value>
            </list>
        </property>
    </bean>

    <!-- Task executor for async cache population -->
    <bean id="taskExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <property name="corePoolSize" value="4" />
        <property name="maxPoolSize" value="20" />
        <property name="queueCapacity" value="25" />
    </bean>

    <task:annotation-driven executor="taskExecutor"/>
</beans>